package com.freelance.platform.entity;

/**
 * Unified role enum for all users in the system.
 * Users can have multiple roles (e.g., an admin can also be a client or freelancer).
 */
public enum Role {
    CLIENT,
    FREELANCER,
    ADMIN
}

